extends: _base.yaml

framework: unsloth
tuning: lora                  # Unsloth LoRA 슬롯(본령). full 은 __full 파일.
image: ghcr.io/jaegookyou/training-framework-comparison-tutorial/unsloth:latest

# LoRA DPO 는 어댑터만 학습 → full 보다 큰 lr (trl lora 와 동일 눈금). ref=어댑터 끈 base.
hp:
  learning_rate: 5.0e-6

# Unsloth 는 단일 GPU 전용.
scale:
  nodes: 1
  gpus: 1
